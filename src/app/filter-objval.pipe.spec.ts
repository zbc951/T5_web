import { FilterObjVal } from './filter-objval.pipe';

describe('FilterObjVal', () => {
  it('create an instance', () => {
    const pipe = new FilterObjVal();
    expect(pipe).toBeTruthy();
  });
});
