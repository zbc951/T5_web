import { ThrottleBtnDirective } from './throttle-btn.directive';

describe('ThrottleBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ThrottleBtnDirective();
    expect(directive).toBeTruthy();
  });
});
